services:
  jellybrarian:
    build: .
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - /mnt/hdd0:/mnt/hdd0
    command: ["-config", "/app/config.toml", "-addr", ":8090"]
